const { ipDetails } = require('./bis_ip_module.service');
module.exports = {

    ipDetails: (req, res) => {
        const body = req.body
        ipDetails(body, (err, results) => {
            if (err) {
                return res.status(200).json({
                    success: 0,
                    message: err
                });
            }

            if (results.length === 0) {
                return res.status(200).json({
                    success: 2,
                    message: "No Results Found"
                });
            }

            return res.status(200).json({
                success: 1,
                data: results
            });
        });
    },

}

